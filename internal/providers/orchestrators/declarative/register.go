package declarative

// Package declarative exists to allow blank imports that ensure declarative orchestrator
// providers register themselves with the builder at init time. The declarative
// orchestrator is implemented in the pipeline/declarative package; this wrapper
// simply provides a stable import path for provider registration in demos.
