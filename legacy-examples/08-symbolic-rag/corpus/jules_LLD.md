---
id: jules_LLD.md
---
# LLD for Manglekit v3.0

The core of the SDK's developer experience is the `Builder`. The primary entrypoint is the `NewBuilder()` function, which provides a fluent API for constructing the orchestrator.