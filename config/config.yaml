orchestrator:
  maxContextTokens: 4000
  fallbackThreshold: 0.5
  intentParser:
    flowName: "parse_intent_ner"
  retrieval:
    path: "docs"
    hybrid:
      bm25:
        topK: 10
      dense:
        topK: 10
  reranker:
    topK: 5

llm:
  provider: "openai"
  model: "gpt-4o-mini"
  apiKey: "${OPENAI_API_KEY}"

embedder:
  provider: "openai"
  model: "text-embedding-3-small"

mangle:
  rulesFile: "config/mangle"