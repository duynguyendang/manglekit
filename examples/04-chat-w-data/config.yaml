rules:
  name: "mangle"
  params:
    path:
      - "mangle_facts.dlog"
      - "mangle_rules.dlog"

retriever:
  name: "in-memory"

llm:
  name: "google"
  params:
    model: "gemini-2.5-flash"
    apiKey: "${GOOGLE_API_KEY}"