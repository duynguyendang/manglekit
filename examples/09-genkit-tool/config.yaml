components:
  - name: "in-memory-retriever"
    kind: "retriever"
    params:
      provider: "in-memory"

  - name: "google-llm"
    kind: "llm"
    params:
      provider: "google"
      model: "gemini-1.5-flash"
      apiKey: "${GOOGLE_API_KEY}"
