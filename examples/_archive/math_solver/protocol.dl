% Declarations to satisfy the Datalog analyzer
status("decl").

% Routing logic
% The engine queries route(Target).

route("verifier") :-
    status("proposed").

route("printer") :-
    status("verified").
